# Test Details Configuration
#
# This file defines the repositories and components that need to be cloned and built
# for conducting each automated test in the GitLab CI pipeline.

tests:

 #------------------------------------ SOFTWARE TESTS ------------------------------------
  software:

    # ----- UNIT TESTS -----
    unit:

      OpenCSD_library_test:
        repositories:
          - name: OpenCSD
            components:
              - libopencsd_dll

    # ----- INTEGRATION TESTS -----
    integration:

 #------------------------------------ HARDWARE TESTS ------------------------------------
  hardware:

    # ----- UNIT TESTS -----
    unit:

    # ----- INTEGRATION TESTS -----
    integration: